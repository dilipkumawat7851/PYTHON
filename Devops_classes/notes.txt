Datatypes:
 
 integer - whole number for example 1,2,3,4
 string  -  sentence , word ,character 
 float   - decimal no for example 1.1,2.1 etc
 Boolean -true or false value

 objects:  -
        ojects are collection of Datatypes


 list
 Dictionary
 set/tuple


Conditinal operator-

== - equal to 
!= - not equal to 
>  - greater then 
<  - less than 
>= - greater then equal to 
<= - less then equal to 


Common python modules

os - operating system
sys - system
re - regular  exprresion
math - Mathemstical  functions
random - random number genrator 
datetime - date and time 
json-javascript object notation


STDIN and STDOUT

STDIN=standard input
STDOUT=Standard output

Try and Except
like i write a code
and the code is not properly run it shows error
In that we can use the try and Except method

What is program?
-- Usually , one or more algorithms written in a programming language that can be translated to run on a real machine
-- We Sometimes call programs software 
--Usually a software has a bunch of programes

language ?

-- A programming language is somewhat like a natural language ,but  eith a very limited set of statement and strict syntax rules.

--has statement to implement sequential, conditional and iterative processing - algorithms

examples  -- FORTRAN, COBOL , Lisp , Basic , Pascal ,  c,  c++ , java ,c# , Python 


Compiler --
-- A Compiler is a program that converts a program written in a programming language into a program in a native language , called machine language
   of the machine that is to execute the proogram 
-- its job like take a language , which is understandable with humans , like c, c++ and turn it 

Interpreter--
--- An alternative to a Compiler is a program called an Interpreter , Rather then converts our program to the language of the computer 
, the Interpreter take our program onr statement ata time and executes a corresponding set of machine instruction 
 
 PYTHON--
 -- python uses an Interpreter . not only can we write complete programes , we can work with the Interpreter in a statement by statement mode enabling us to experiment quite easily 
 -- python is especially good for our purpose in that it does not have a lot of overhead before getting starte
 --it is easy to jump in and experiment with python in an interactive fashion 


functions
-- functions are also called first class object 

 



